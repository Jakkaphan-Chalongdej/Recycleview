<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cooking">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_baker"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreenActivity" android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" android:theme="@style/AppTheme2"></activity>
        <activity android:name=".ViewEat" android:label="อาหารคาว" android:theme="@style/AppTheme3"></activity>
        <activity android:name=".ViewEatSweet" android:label="ของหวาน" android:theme="@style/AppTheme4"></activity>
        <activity android:name=".ViewDrink" android:label="เตรื่องดื่ม" android:theme="@style/AppTheme5"></activity>
        <activity android:name=".InformationActivity"></activity>


    </application>

</manifest>